# Phase 18BS — Trendyol public artifact field boundary

Date: 2026-08-28

## Goal

Follow Phase 18BR by probing materially new public screenshots, help/cache/search-index and technical-artifact surfaces around Trendyol Azerbaijan's `1001 Sevinc` integration. Primary target: an absolute finite-pool denominator for current `10066 Silver` / `10072 S25` (`remaining`, `total`, issuance count, inventory/stock, or a directly interpretable equivalent). Portfolio size **N remains a free integer optimization variable**.

## New evidence

### 1. Public Trendyol screenshot artifact recovered

A web image-index query surfaced a real Trendyol Azerbaijan order-confirmation screenshot hosted in a Birbank/Trendyol context. The screenshot contains a `1001 Sevinc` promotional banner at the top of a successful Trendyol order-confirmation screen and a visible `BİLET AL` call-to-action.

This materially extends the known Trendyol surface map beyond `Sifarişlərim -> 1001 Sevinc`: the lottery is also injected into ordinary post-purchase/order-confirmation UI as an acquisition banner.

Observed banner/UI content is promotional only. It exposes no `remaining`, `total`, issuance count, sold count, inventory, draw-specific cap, or sold percentage.

### 2. Public/indexed account-field boundary remains narrow

Fresh Azerbaijani/Russian/English searches for combinations of `1001 Sevinc`, Trendyol, `remaining`, `qalan`, `ticketStatus`, `drawDate`, API/payload and GitHub/code artifacts returned no public payload or screenshot with absolute inventory semantics.

The strongest repeatedly indexed partnership wording still says that purchased-ticket information visible in the Trendyol account consists of:

- ticket status;
- draw date;
- lottery result.

No indexed first/second-party source adds `remaining`, `total`, `stock`, `inventory`, `issued`, or similar fields to that public description.

### 3. First-party Azerlotereya mechanics reconfirm denominator existence, but not its value

A fresh first-party Azerlotereya explainer still states that the Azerlotereya.com / Misli.az `1001 Sevinc` surface can show how many tickets remain until a draw, and that sales stop when the predetermined ticket count is reached. This reconfirms that an absolute denominator exists operationally for the draw/prize-category scope.

However, no public Trendyol artifact found in this batch exposes that same field or lets us map a current `remaining` value to `10066` or `10072`.

## Interpretation

The Trendyol route remains real, but this batch establishes a useful **public-artifact boundary**:

- public launch/press text: `status`, `draw date`, `result`;
- publicly indexed order-confirmation screenshot: acquisition banner only;
- no public/indexed evidence yet that the Trendyol ticket card itself renders the denominator;
- no discoverable public API/schema/payload artifact tied to `1001 Sevinc` exposed denominator-like fields.

Therefore **do not spend another batch on generic Trendyol web/image/search-index permutations** unless a materially new screenshot, authenticated capture, app payload, support/help artifact, or endpoint surface appears. The remaining high-value Trendyol sub-route is authenticated/runtime evidence, not repeated public indexing.

## Candidate impact

No cap/remaining recovered, so no live ROI recalculation is justified.

- `10066 Silver`: still denominator target #1; existing fresh first-party sold% = 33%.
- `10072 S25 Ultra Black`: denominator target #2; fresh sold% unresolved.
- `N`: remains free integer.
- `operational_integrity_status`: unchanged / separately unresolved.

## NEXT ACTION

1. Shift denominator priority away from generic Trendyol public indexing to **new authenticated/runtime/rendered evidence** on Azerlotereya/Misli/Trendyol, plus retail/POS artifacts.
2. For Trendyol specifically, only revisit on a materially new account screenshot, app payload/network capture, support/help article, or directly interpretable ticket-card artifact.
3. Continue searching for current `remaining/total` bound to `(drawId, prize, ticket price, draw date, source surface, crawl timestamp)` for `10066` and `10072`.
4. If `C` and `R` or exact sold tickets `M` appear, compute buffered ROI immediately at 60/70/80/100% usable value and the standing 14% property-prize tax model.
5. Preserve free integer `N`; do not reopen closed draw-history prediction branches.

## Result

**Not yet success.** New public Trendyol UI surface confirmed, but no absolute denominator recovered. Generic public Trendyol indexing is now deprioritized until materially new evidence appears.
