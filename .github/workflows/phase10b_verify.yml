name: Phase 10B Ensemble Verify

on:
  push:
    paths:
      - 'experiments/phase10b_ensemble_verify.py'
      - '.github/workflows/phase10b_verify.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-phase10b:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run Phase 10B
        run: python experiments/phase10b_ensemble_verify.py
      - name: Commit results
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add results/phase10b_ensemble_verify.json results/PHASE10B_ENSEMBLE_VERIFY.md
          if git diff --cached --quiet; then
            echo 'No result changes'
          else
            git commit -m 'Save Phase 10B ensemble verification'
            git push
          fi
# trigger: phase10b-initial-run
